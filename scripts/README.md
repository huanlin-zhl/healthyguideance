# 工程脚本

放置本地开发与 CI 用脚本：

- `build-backend.ps1` — 构建后端解决方案
- `export-openapi.ps1` — 启动 API 并导出 OpenAPI 到 `shared/api-schema/openapi.json`
- `build-client-windows.ps1` — 构建 WinUI 3 客户端
- `package-windows.ps1` — 打包 MSIX

（脚本待补）
