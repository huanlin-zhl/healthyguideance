<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="HealthyGuidance.App.Pages.NotesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24,16,24,0" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="备注"
                   FontSize="28"
                   FontWeight="SemiBold" />

        <!-- 顶部输入区 -->
        <Border Grid.Row="1"
                Padding="16"
                CornerRadius="8"
                BorderThickness="1"
                BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}">
            <StackPanel Spacing="8">
                <Grid ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                        <TextBlock Text="时间：" VerticalAlignment="Center" Opacity="0.7" />
                        <CalendarDatePicker x:Name="NoteDatePicker" Date="{x:Null}" PlaceholderText="今天" />
                        <TimePicker x:Name="NoteTimePicker" ClockIdentifier="24HourClock" />
                    </StackPanel>
                </Grid>
                <TextBox x:Name="NoteInputBox"
                         PlaceholderText="今天中午吃了..."
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         MinHeight="100" />
                <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right">
                    <TextBlock x:Name="StatusText" VerticalAlignment="Center" Opacity="0.7" />
                    <Button x:Name="SaveButton"
                            Content="保存"
                            Style="{ThemeResource AccentButtonStyle}"
                            Click="SaveButton_Click" />
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- 历史列表 -->
        <ScrollViewer Grid.Row="2"
                      VerticalScrollBarVisibility="Auto"
                      Padding="0,0,0,16">
            <StackPanel x:Name="MonthsPanel" Spacing="12" />
        </ScrollViewer>
    </Grid>
</Page>
